/**
 * Test case for staticMiddleware.
 * Runs with mocha.
 */
'use strict'

const staticMiddleware = require('../lib/middlewares/static_middleware.js')
const assert = require('assert')
const co = require('co')

describe('static-middleware', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Static middleware', () => co(function * () {

  }))
})

/* global describe, before, after, it */
